# 变限积分

### 概述

变限积分是一种自变量为积分上（下）限的函数：
$$
F(x)=\int_a^xf(t)dt
$$
### 变限积分的导数

首先，变限积分一定是连续的。这是由 [[一致连续性]] 保证的
- [x] 补充变限积分连续性的证明 📅 2026-01-07 ✅ 2026-01-08

进一步地，如果变限积分还可导的话，有如下式子：
$$
\begin{align}
\frac{\text{d}}{\text{d}x}\int_a^xf(t)dt&=\lim_{ h \to 0 } \frac{\int_a^{x+h}f(t)dt-\int_a^{x}f(t)dt}{h} \\
&=\lim_{ h \to 0 } \frac{\int_x^{x+h}f(t)dt}{h}
\end{align}
$$
这个形式是一定对的，而且还有点用处。它还可以继续往下推，由连续性：
$$
\forall\varepsilon>0,\forall t\in[x-\varepsilon, x+\varepsilon],f(x)-\varepsilon<f(t)<f(x)+\varepsilon
$$
于是有：
$$
\int_x^{x+h}[f(x)-\varepsilon]dt<\int_x^{x+h}f(t)dt<\int_x^{x+h}[f(x)+\varepsilon]dt
$$
这也即：
$$
h[f(x)-\varepsilon]<\int_x^{x+h}f(t)dt<h[f(x)+\varepsilon]
$$
**注**：这里是把 $t$ 当成了变量，$x$ 当成常数，然后相当于是：$\int_a^bc\text{d}t=(b-a)c$ 一样

于是对于上边的原式有：
$$
f(x)-\varepsilon<\lim_{ h \to 0 } \frac{\int_x^{x+h}f(t)dt}{h}<f(x)+\varepsilon
$$
这也即：
$$
\lim_{ h \to 0 } \frac{\int_x^{x+h}f(t)dt}{h}=f(x)
$$
这说明，变限积分函数的导数值等于该点的函数值

